export type Wallet = {
  address: string;
  privateKey: string;
  mnemonic: string;
  satoshis?: number;
};

export type WalletKeys = {
  address: string;
  privateKey: string;
  satoshis?: number;
};

